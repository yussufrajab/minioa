# CSMS Database Configuration
# Copy this file to frontend/.env for Next.js application

# Database Configuration
DATABASE_URL="postgresql://postgres:Mamlaka2020@localhost:5432/prizma"
DIRECT_URL="postgresql://postgres:Mamlaka2020@localhost:5432/prizma"

# Next.js Configuration
NEXTAUTH_URL="http://localhost:9002"
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# API Configuration
NEXT_PUBLIC_API_URL="http://localhost:8080/api"

# Application Settings
NODE_ENV="development"
PORT=9002

# File Upload Settings (max 2MB per file)
MAX_FILE_SIZE=2097152

# JWT Configuration (matches Spring Boot backend)
JWT_ACCESS_TOKEN_EXPIRY=600000
JWT_REFRESH_TOKEN_EXPIRY=86400000

# Database Connection Pool
DB_CONNECTION_TIMEOUT=20000
DB_IDLE_TIMEOUT=10000
DB_POOL_SIZE=10

# Logging
LOG_LEVEL="info"

# Backend Server Configuration (for reference)
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/prizma
# SPRING_DATASOURCE_USERNAME=postgres  
# SPRING_DATASOURCE_PASSWORD=Mamlaka2020
# SPRING_JPA_HIBERNATE_DDL_AUTO=validate
# SPRING_PROFILES_ACTIVE=dev
# SERVER_PORT=8080
# SERVER_SERVLET_CONTEXT_PATH=/api

# Security Configuration
BCRYPT_ROUNDS=12
SESSION_SECRET="your-session-secret-here"

# File Storage
UPLOAD_DIR="./uploads"
TEMP_DIR="./temp"

# Email Configuration (if needed)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# Development Tools
PRISMA_STUDIO_PORT=5555